function sendWhatsApp(to, template, payload = {}) {
  // Stub for WhatsApp automation; replace with provider integration (e.g., Meta WhatsApp Cloud API)
  console.log("[WhatsApp]", { to, template, payload });
}

module.exports = { sendWhatsApp };